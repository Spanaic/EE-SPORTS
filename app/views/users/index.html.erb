<div class="container">
    <div class="row">
        <div class="col-md-12 index-users-main-box">
            <p style="font-weight: bold; padding-top: 10px;">おすすめ</p>
            <div class="index-users-big-box">
                <% @users.each do |user| %>
                    <div class="index-users-small-box">
                        <div class="index-users-item">
                            <%= link_to user_path(user.profile_name) do %>
                                <%= attachment_image_tag user, :profile_image, :fill, 40, 40, format: 'jpeg', size: '40x40', fallback: 'no_image.jpg', class: "profile-image-style" %>
                            <% end %>
                        </div>
                        <div class="index-users-item">
                            <%= link_to user_path(user.profile_name) do %>
                                <%= user.profile_name %>
                            <% end %>
                        </div>
                        <div class="index-users-item">
                            <%= link_to user_path(user.profile_name) do %>
                                <%= "#{user.name}" %>
                            <% end %>
                        </div>
                        <div class="index-users-item all-users-follow-button">
                            <%= link_to user_path(user.profile_name) do %>
                                <button><%= link_to "フォローする", user_relationships_path(user.profile_name), method: :post %></button>
                            <% end %>
                        </div>
                　　</div>
                    <div class="index-users-border-line"></div>
                <% end %>
            </div>
        </div>
    </div>
</div>